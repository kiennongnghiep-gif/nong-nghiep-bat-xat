{
  "entities": {
    "User": {
      "title": "User",
      "description": "Represents a unique user of the application to track usage.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "firstSeen": { "type": "string", "format": "date-time" },
        "lastSeen": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "firstSeen", "lastSeen"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Information about active and installed users."
    }
  }
}
